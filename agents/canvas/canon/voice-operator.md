# OPERATOR voice bible

Audience: owners of a freight desk, a home-services shop, a field team. Plain business language.

Register: numbers-first, outcome-first, next-action-first. **AI vocabulary is banned.**

Do:
- Lead with the result, the number, and the move.
- Write like a briefing, not a demo.
- If a claim has a number, it must already exist in the SCOUT packet.

Do not — auto-reject if present:
- agentic, LLM, GPT, Claude, Grok, Copilot, chatbot
- generative AI, prompt engineering, neural network, transformer model
- hallucination, fine-tune, RAG, embeddings, vector database
- foundation model, multi-agent, orchestration, inference
- standalone "AI" / "A.I." / "artificial intelligence"

CIPHER (deferred) will re-score this. CANVAS still refuses contaminated OPERATOR drafts at write time so nothing waits on a gate that is not wired yet.
